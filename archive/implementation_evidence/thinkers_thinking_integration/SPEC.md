# SPEC

## Goal

Connect the Judge-pass Thinkers OS Lens Router, Conflict Map, and active provisional synthesis patterns to `[Thinking]` through the existing bundle-first repository model.

## Current state

- `[Thinking]` has three validated Knowledge bundles and an authoritative upload list.
- The four named granular Thinkers files and `THINKING_04_THINKERS_SYNTHESIS.md` do not exist in AI-OS; no canonical equivalents were found.
- `[Thinkers OS]` records five active provisional synthesis patterns, a Lens Router, a Conflict Map, source-artifact IDs, precedence, export boundaries, and rollback.
- The upstream Thinkers OS source repository contains the corresponding Judge-pass synthesis registry, Judge review, Thinking projections, and five retrospective application records.
- Baseline repository checks pass: seven Project Instructions, 122 manifest/path checks, 32 bundles, nine index pairs, and 56 pytest tests.

## Requirements

- Add the four named granular Thinking artifacts without importing raw sources, manifests, or application-history payloads.
- Add `THINKING_04_THINKERS_SYNTHESIS.md` with five active provisional patterns, Lens Router, Conflict Map, precedence, anti-bloat, logging, routing, safety, and rollback contracts.
- Keep `[Thinking]` responsible for applying patterns to real decisions; corpus, source intake, synthesis status, and author artifact maintenance remain in `[Thinkers OS]`.
- Add the bundle to the authoritative Thinking upload list and update the Knowledge index, bundle metadata, fingerprints, status/setup guidance, and repository smoke evidence.
- Add regression coverage for the seven requested smoke cases and content-safety boundaries.
- Commit and push a non-main feature branch without merge or external ChatGPT changes.

## Constraints

- Do not add raw or normalized books, excerpt dumps, source/acquisition manifests, execution logs, retrospective application entries, blocked/rejected artifacts, secrets, or local absolute paths.
- Do not copy the upstream writable pattern registry into AI-OS or independently change canonical synthesis status.
- Do not activate pilot candidate revisions that lack a separate Judge decision.
- Preserve `[Analytics]`, `[LLM]`, and `[Codex]` routing.
- Owner acceptance remains pending; production status remains `NOT AUTHORIZED`.

## Acceptance criteria

1. All five named Thinking artifacts exist and are indexed; no duplicate equivalent exists.
2. Bundle 04 contains exactly five active provisional synthesis patterns and the requested operational contracts.
3. Source provenance names Judge-pass author pattern/Judge IDs and the Thinkers OS synthesis status source without embedding source payloads.
4. The application log is an empty append-only schema; bundle content includes only the logging rule, not case history.
5. `UPLOAD_LIST.md` lists bundle 04 and remains the authoritative bounded upload list.
6. Project Instructions and setup/status guidance make the router operational while preserving project ownership boundaries.
7. Seven requested smoke contracts pass, including 2–4 lens selection and preserved routing.
8. Bundle fingerprints, Knowledge index coverage, safety checks, all repository validators, and full pytest pass.
9. Changes are committed and pushed to a non-main feature branch without merge.
10. External ChatGPT Projects remain unchanged; owner acceptance is pending and production is `NOT AUTHORIZED`.

## Risks

- Copying upstream candidate revisions could silently alter active synthesis without Judge authorization.
- Adding all source authors as active lenses could violate anti-bloat and degrade simple decisions.
- Changing the Knowledge index requires regenerating the existing Thinking bundle 01 fingerprint and its compact index mirror.
- Static repository smoke cannot prove external ChatGPT behavior before manual sync.
